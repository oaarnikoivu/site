export type Experience = {
  icon: React.ReactNode;
  company: string;
  title: string;
  date: string;
  linkTo: string;
};

export type Social = {
  icon: React.ReactNode;
  name: string;
  linkTo: string;
};
